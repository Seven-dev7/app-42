<h1> Articles </h1>

	<% @posts.each do |post| %>
		<h2> <%= post.title %> </h2>
		<p> <%= post.content %></p>

		<p>
			<a href="<%= posts_path(post.id) %>", class="btn btn-primary">Lire la suite </a>
		</p>
		<%end%>
<h1> <%= @group.title %></h1>

<p>
	<%= @group.description %>
</p>

<p>
	<a href="<%= edit_group_path(@group.id) %>", class="btn btn-primary"> Editer </a>
	<a href="<%= groups_path %>", class="btn btn-success"> Accueil </a>
</p>

